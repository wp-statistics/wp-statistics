name: PHPStan

on:
  pull_request:
    branches:
      - master
      - development

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - uses: dingo-d/phpstan-wp-action@v2
